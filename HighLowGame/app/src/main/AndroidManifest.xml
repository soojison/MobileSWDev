<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.soojison.highlowgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        <!-- android:clearTaskOnLaunch="true"> peekler: *confused this with nohistory* oh no I have drank too much beer
                                               use this to not save this in the backstack at all-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameActivity" />
        <activity android:name=".ResultActivity"
            android:theme="@style/Theme.AppCompat.Dialog"></activity> <!--- make the activity be a dialog -->
    </application>

</manifest>